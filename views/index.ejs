<%- include('./includes/header') %>
<link rel="stylesheet" href="/style/main.css">

</head>
<body>
    <%- include('./includes/navbar') %>

    <h1><%= pageTitle %></h1>
    <img class="img1" src="/images/pexels-lisa-fotios-7934094 (2).jpg"> <!--記住相對路徑才不會報錯-->

<!--TODO: 使用迴圈跑出 products 資料 

    <% if (products.length > 0) { %> 
    <% } else { %>
        尚無商品
    <% } %> 
-->

<!-- TODO: 使用迴圈帶入資料跑出 products 資料 -->
    <!-- <% if (products.length > 0) { %>
        <% products.forEach((product) => { %>
            <img src="<%= product.imageUrl %>" alt="<%= product.title %>">
            <h3><%= product.title %></h3>
        <% }) %>
    <% } else { %>
        尚無商品
    <% } %> -->

<!--使用 BS5 card component-->
<div class="container-md">
    <h1><%= pageTitle %></h1>
    <div class="row">
        <% if (products.length > 0) { %>
            <% products.forEach((product) => { %>
                <div class="col-12 col-md-6 col-lg-4 g-3">
                    <div class="card h-100">
                        <img src="<%= product.imageUrl %>" class="card-img-top" alt="<%= product.title %>">
                        <div class="card-body">
                          <h5 class="card-title"><%= product.title %></h5>
                          <p class="card-text"><%= product.description %></p>
                          <a href="#" class="btn btn-primary">詳細資訊</a>
                        </div>
                      </div>
                </div>
            <% }) %>
        <% } else { %>
            <h1>尚無商品</h1>
        <% } %>
    </div>

</div>

    
</body>
</html>

    <%- include('./includes/footer') %> 
